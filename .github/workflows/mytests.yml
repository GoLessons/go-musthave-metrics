name: myself learning tests

on:
  pull_request:

jobs:
  myself-test:
    runs-on: ubuntu-latest
    container: golang:1.24

#    services:
#      postgres:
#        image: postgres
#        env:
#          POSTGRES_PASSWORD: postgres
#          POSTGRES_DB: praktikum
#        options: >-
#          --health-cmd pg_isready
#          --health-interval 5s
#          --health-timeout 5s
#          --health-retries 5

    steps:
      - name: "Myself tests"
        run: |
          go test -v -race -tags testmode ./...
